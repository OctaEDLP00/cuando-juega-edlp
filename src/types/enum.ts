export enum RULES {
  ON = 'on',
  OFF = 'off',
  WARN = 'warn',
  ERROR = 'error',
  ALWAYS = 'always',
  NEVER = 'never'
}

export enum TARGET {
  BLANK = '_blank',
  PARENT = '_parent',
  SELF = '_self',
  TOP = '_top'
}
export enum REL {
  NOOPENERNOREFERRER = 'noopener noreferrer',
  NOOPENER = 'noopener',
  NOREFERRER = 'noreferrer'
}
