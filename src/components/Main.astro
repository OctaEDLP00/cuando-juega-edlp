---
import Card from './Card'
import { fetchAPI } from '@utils/fetchAPI'
import { URL_API_ENDPOINT_2023 } from '../const/index'

const partidos = await fetchAPI(URL_API_ENDPOINT_2023)
---

<main
	class='flex flex-col items-center justify-center gap-4 pt-16 w-full mt-16 md:mt-32 overflow-x-hidden md:flex-row md:flex-wrap md:justify-center md:lg:xl:pt-24'
>
	<div
		id='cards'
		class='grid gap-4 place-content-center grid-cols-1 justify-items-center w-[1100px] h-full md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 p-4 m-4'
	>
		{
			partidos.map(({ timestamp, copa, img, vs }) => (
				<Card
					client:load
					transition:animate='slide'
					transition:name={vs}
					key={vs}
					img={img}
					copa={copa}
					timestamp={timestamp}
					vs={vs}
				/>
			))
		}
	</div>
</main>
